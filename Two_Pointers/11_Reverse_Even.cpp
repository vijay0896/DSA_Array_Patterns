//  12. Reverse only Even Index Elements
// Input: [1, 2, 3, 4] → Output: [4, 2, 3, 1]

#include <iostream>
using namespace std;
void reverseEven(int arr[], int size)
{

    int left = 0;
    int right = size - 1;
    while(left<=right){
        
    }
}

int main()
{

    int arr[] = {1, 2, 3, 4};
    int size = sizeof(arr) / sizeof(arr[0]);
    reverseEven(arr, size);
    return 0;
}